spring:
  application:
    name: "backend"
  config:
    activate:
      on-profile: "test"

  flyway:
    enabled: false

mybatis:
  mapper-locations: classpath:mappers/*.xml
  configuration:
    map-underscore-to-camel-case: true
  type-handlers-package: com.yachaerang.backend.global.handler